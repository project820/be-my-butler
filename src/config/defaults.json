{
  "version": 1,
  "git": {
    "auto_push": "ask",
    "auto_commit": true
  },
  "cross_model": {
    "provider": "codex",
    "codex_model": "LATEST",
    "gemini_model": "LATEST",
    "timeout_seconds": 3600,
    "_note": "LATEST = use provider CLI default (best available model). Override with specific model name if needed."
  },
  "timeouts": {
    "claude_agent": 1200,
    "cross_model": 3600,
    "writer": 600,
    "_note": "Seconds. claude_agent applies to executor/tester/verifier/simplifier. Adjustable via /BMB-setup."
  },
  "consultant": {
    "style": "default",
    "custom_style": null,
    "language": "en"
  },
  "notifications": {
    "telegram": {
      "enabled": false
    }
  }
}
